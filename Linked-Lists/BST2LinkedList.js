function TreeNode(value, left, right) {
  this.left = left;
  this.right = right;
  this.value = value;
}

function ListNode(value, next, prev){
  this.value = value;
  this.next = next;
  this. prev = prev;
}

function LinkeList(head, tail)